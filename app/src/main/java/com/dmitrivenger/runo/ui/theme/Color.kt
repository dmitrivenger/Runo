package com.dmitrivenger.runo.ui.theme

import androidx.compose.ui.graphics.Color

// ── Dark mode ────────────────────────────────────────────────────────────────
val Dark_Background     = Color(0xFF0D0D0D)
val Dark_Surface        = Color(0xFF161616)
val Dark_SurfaceVariant = Color(0xFF222222)
val Dark_OnBackground   = Color(0xFFEBEBEB)
val Dark_OnSurface      = Color(0xFFEBEBEB)
val Dark_OnSurfaceMuted = Color(0xFF666666)
val Dark_Border         = Color(0xFF2E2E2E)

// ── Light mode ───────────────────────────────────────────────────────────────
val Light_Background     = Color(0xFFF5F5F0)
val Light_Surface        = Color(0xFFFFFFFF)
val Light_SurfaceVariant = Color(0xFFEBEBEB)
val Light_OnBackground   = Color(0xFF111111)
val Light_OnSurface      = Color(0xFF111111)
val Light_OnSurfaceMuted = Color(0xFF6B7280)
val Light_Border         = Color(0xFFE0E0E0)

// ── Brand colours ─────────────────────────────────────────────────────────────
// Brand_Forest: primary UI elements — buttons, FAB, active nav, toggles ON
val Brand_Forest        = Color(0xFF166534)
val Brand_ForestVariant = Color(0xFF14532D)   // pressed state / primaryContainer
// Brand_Lime: data visualisation ONLY — route polyline, bar chart bars (NOT buttons)
val Brand_Lime          = Color(0xFF22C55E)
val Brand_Blue          = Color(0xFF3B82F6)
val Brand_Red           = Color(0xFFEF4444)
// Text on Brand_Forest buttons is white in both dark and light mode
val Brand_OnForest      = Color(0xFFFFFFFF)
